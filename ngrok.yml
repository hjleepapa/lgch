version: "2"
authtoken: # Your ngrok authtoken here (optional but recommended)
tunnels:
  flask:
    proto: http
    addr: 5000
    schemes: [https]
  websocket:
    proto: http
    addr: 5001
    schemes: [https]
